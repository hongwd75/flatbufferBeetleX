namespace NetworkMessage;

//-------------------------------------------------------
enum eChatType : byte {
	CT_System = 0x00,
	CT_Say,
	CT_Chat,
	CT_Guild,
	CT_Help
}
enum eChatLoc : byte {
	CL_ChatWindow = 0x0,
	CL_PopupWindow = 0x1,
	CL_SystemWindow = 0x2
}
//-------------------------------------------------------	

enum ServerPackets : ushort {
	SC_LoginAns	= 1,
	SC_AccountInfo,
	SC_StringMessage
}

enum ClientPackets : ushort {
	
	CS_LoginReq = 1,
	CS_WorldJoinReq,
	CS_UpdatePosition,
}